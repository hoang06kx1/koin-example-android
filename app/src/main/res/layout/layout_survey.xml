<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:scaleType="centerCrop"
        android:id="@+id/img_survey"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        style="@style/TextSurveyTitle"
        android:id="@+id/tv_survey_title"
        android:paddingStart="@dimen/list_survey_text_padding"
        android:paddingEnd="@dimen/list_survey_text_padding"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.25"
        tools:text="Ocpatus" />

    <TextView
        style="@style/TextSurveyDescription"
        android:id="@+id/tv_survey_desc"
        android:layout_marginTop="12dp"
        android:paddingStart="@dimen/list_survey_text_padding"
        android:paddingEnd="@dimen/list_survey_text_padding"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_survey_title"
        tools:text="lorem ipsum ali" />

</androidx.constraintlayout.widget.ConstraintLayout>